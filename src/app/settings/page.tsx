"use client"

import { Button } from "@/components/ui/button"
import { ModeToggle } from "@/components/ui/modetoggle"

export default function Settings() {
    return (
        <div>
            <p> This is an example of how the <i> Settings </i> should look. </p>
            <Button> Based </Button>
        </div>
    )
}